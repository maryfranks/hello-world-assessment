<%= form_tag post_replies_path(post.id) do %>
  <div class="reply-form">
    <%= label_tag :reply %><br/>
    <%= text_field_tag :reply, params[:reply] %>
    <%= submit_tag "Reply" %>
  </div>
<% end %>
